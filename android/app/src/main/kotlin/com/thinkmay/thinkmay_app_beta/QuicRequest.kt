package com.thinkmay.thinkmay_app_beta

class QuicRequest {
    compation object {
        @JvmStatic
        private external fun quicRequest(pattern: String): String) 
    }
    
    fun sendQuicRequest(to: String): String {
        return quicRequest(to)
    }
}